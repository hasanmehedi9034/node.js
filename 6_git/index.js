/*
setup git:
    git config --global user.name 'username'
    git config --global user.email 'email'
    git config --list (for showing detail with git enviornment)

repo:
    git init
    git status
    git log or git log --oneline
    git checkout commitnumber filename
    git checkout master (for detached head)
    git reset filename (for unstage a file)

onlie rep:
    showing details in website
    git push -u origin master

----------------------------------------------------------------
ignore a file or folder:
    create a file which name is .gitignore
    and write filename or folder name, which you want to ignore

*/